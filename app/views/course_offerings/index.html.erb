<%= render 'terms/terms_dropdown', terms: @terms, term_name: @term_name%>
<%= render 'search_form' %>

<p id="notice"><%= notice %></p>
<%= render partial: 'page_buttons', locals: { pagination: @pagination } %>
<h1>Course Offerings</h1>
<% @course_offerings.each do |course_offering| %>
  <%= render 'course_offering', course_offering: course_offering  %>
<% end %>